##############################################
This code creates EC2-Ubuntu instances using aws instance module defination in module_folder.

Module folder:
It has code to create aws ec2 instance. Code is parameterized. We need to pass ami and instance type in order to create instance.

Steps to run the code to create infrastructure:
1. terraform init - It downloads the aws provider for Terraform.
2. terraform plan - To preview the infrastructure being created.
3. terraform apply - To create the infrastructure.

